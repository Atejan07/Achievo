const express = require('express');
const router = express.Router();
const controller = require ('../controllers/controller');
// const authMiddleware
  

// router.get('/', controller);
// router.post('/books', controller);

  

module.exports = router;